{
  "name": "@valkyr/docs",
  "private": true,
  "scripts": {
    "write": "contented write",
    "generate": "contented generate",
    "build": "contented build",
    "clean": "rm -rf dist && rm -rf .contented"
  },
  "devDependencies": {
    "@birthdayresearch/contented": "1.8.8"
  }
}