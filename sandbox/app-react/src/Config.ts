export const config = {
  /*
   |--------------------------------------------------------------------------------
   | Auth
   |--------------------------------------------------------------------------------
   */

  api: {
    endpoint: {
      http: process.env.api,
      socket: process.env.socket
    }
  },

  /*
   |--------------------------------------------------------------------------------
   | Auth
   |--------------------------------------------------------------------------------
   */

  auth: {
    endpoint: {
      signin: "/signin",
      signup: "/signup",
      reset: "/password"
    }
  },

  /*
   |--------------------------------------------------------------------------------
   | Environment
   |--------------------------------------------------------------------------------
   */

  environment: {
    current: process.env.environment,
    is(env: Environment) {
      return env === process.env.environment;
    }
  },

  cloudinary: {
    account: "cmdoapps",
    key: "393356327488916",
    secret: "7Y6bDUQgWj75cuXInau7klJli5k"
  }
};

/*
 |--------------------------------------------------------------------------------
 | Types
 |--------------------------------------------------------------------------------
 */

export type Environment = "production" | "development";
