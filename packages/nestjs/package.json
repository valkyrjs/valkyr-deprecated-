{
  "name": "@valkyr/nestjs",
  "version": "0.0.0",
  "description": "Toolkit for building @valkyr nestjs based apis.",
  "repository": "kodemon/valkyr",
  "bugs": "https://github.com/kodemon/valkyr/issues",
  "license": "MIT",
  "main": "src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b ./tsconfig.build.json",
    "flush": "npm run clean && rm -rf ./node_modules",
    "clean": "rm -rf .turbo && rm -rf dist && rm -f tsconfig.build.tsbuildinfo"
  },
  "dependencies": {
    "@nestjs/common": "^9.2.1",
    "class-validator": "^0.14.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.15"
  }
}
